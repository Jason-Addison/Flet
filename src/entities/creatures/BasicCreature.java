package entities.creatures;

import java.awt.image.BufferedImage;

import entities.Entity;

public class BasicCreature extends Entity
{

	public BasicCreature(BufferedImage texture, int health)
	{
		super(texture, health);
	}
	
}
